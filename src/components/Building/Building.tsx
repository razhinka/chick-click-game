import React from 'react';
import PropTypes from 'prop-types';
import { player } from '../../context/player-context';

const Building = () => (
 <div>
    Building Component
 </div>
);

Building.propTypes = {};

Building.defaultProps = {player};

export default Building;
